# Test that we can start the gopm server and shut it down cleanly.
gopm -c gopmconfig --quit-delay 0 &
waitfile gopm.socket
gopmctl --addr unix:gopm.socket shutdown
wait
-- gopmconfig/config.cue --
package config

grpc_server: {
	network: "unix"
	address: "gopm.socket"
}
